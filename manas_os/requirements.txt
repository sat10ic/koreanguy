# Manas AI Trading OS — backend dependencies
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pandas>=2.2
numpy>=1.26
requests>=2.31
PyYAML>=6.0
python-dateutil>=2.9
# Fyers market data (adopted provider)
fyers-apiv3>=3.0.0
# dev/test + lint
pytest>=8.0
ruff>=0.4
# SHIP-1 #7: ML direction classifier (EXPERIMENTAL, optional — the
# ml_direction pipeline stage / manas_os.ml.direction_lgbm skips
# failure-safe when these are not installed)
lightgbm>=4.0
shap>=0.45
# SHIP-1 #17 (I5): HMM regime confirmation gate (EXPERIMENTAL, optional — the
# regime_hmm nightly stage / manas_os.regime.regime_hmm skips failure-safe
# when this is not installed). Install with `python -m pip install hmmlearn`
# (plain `pip` can target the wrong interpreter on this machine).
hmmlearn>=0.3
