# FUTURE — deferred scope (do NOT build until the core loop is proven)

The core loop = P0 Foundation → P1 Regime page → P2 Scanner+Readiness+Risk+minimal journal
→ P3 Chart presets + Pine ports + full journal + EOD alerts → P4 live intraday Strong Start.

Everything below is parked by deliberate decision (2026-07-04). Anything tempting to add
mid-build goes here first and waits.

## Quant / risk-science layer
- Markov / HMM regime transition model.
- Monte-Carlo drawdown / risk-of-ruin simulator.
- Fractional Kelly position-size advisor.
- Backtest + walk-forward validation engine.

## Intraday (beyond Strong Start)
- Busted 15-minute reversal engine.

## Data surfaces already captured but not yet surfaced
- The 24 extra ChartsMaze technical screeners as UI surfaces.
- The 5 named-trader templates (chhirag/himanshu/hiren/nitin/shashank) as UI surfaces.
- Disclosure feeds (ASM, bulk/block deals, insider, corporate announcements) as smart-money views.

## AI layer
- AI journal coach + narrative intelligence (provider chosen when this is built).

## Regime
- EM sheet dial (until the user shares access) — added as a 2nd dial + offline cross-check,
  never the sole headline number. XP stays canonical.

## Other
- Institutional-flow module (FII/DII/pledge).
- Multi-timeframe (MTF) alignment module.
- Chart preset cloning / export / permalink / historical replay.
- Options / F&O, auto-execution, any broker order routing (permanently excluded from MVP).
