# WAVE 2 SPEC — discovery + breadth-fill (Codex, after wave 1 QC)

Repo C:\Users\satta\Downloads\koreanguy, branch emergent. Backend Python only (no desk edits —
avoid collision with wave-1 frontend). Set PYTHONPATH=repo root for python -m. No money-math
changes. No git commit. pytest green + report per item.

## B. Breadth-fill (user 2026-07-18: "Monthly move breadth & DMA-cross empty, we have bhavcopy")
VERIFIED: breadth_daily has the columns but 0/1242 non-null — they were wired to the external
breadth sheet which lacks them. Compute from daily_prices (EQ) instead, in
manas_os/sources/breadth_counts.py (already computes breadth from daily_prices):
- pct_10dma_gt_20dma = 100 * (# symbols with SMA10 > SMA20) / (# with both defined)
- pct_20dma_gt_40dma = 100 * (# with SMA20 > SMA40) / (# with both)
- up_25pct_month / down_25pct_month = # symbols whose close is >= +25% / <= -25% vs its close ~21
  trading sessions ago (use the actual bar 21 rows back per symbol, not calendar)
- up_50pct_month / down_50pct_month = same at +/-50%
Universe = the same EQ, close>=1 set breadth_counts already uses. Write these into breadth_daily
for run_date (UPSERT the existing row, don't duplicate). Add a one-time backfill path
(`python -m ... breadth-backfill` OR a function callable for the last N=250 sessions) so the
history charts fill, not just today. Idempotent.
Done-test: after run, breadth_daily non-null count for all 6 fields > 0 for recent dates; the
/api/regime/breadth-analytics payload carries them; MarketHomeTab hasMonthly/hasDmaCross become true.

## A. Discovery / Manas-picks (from AUDIT_LEDGER I7/I8 + probe_labels findings)
Nine of 13 practitioner picks refused; 3 fixable gate classes + broken discovery bucket:
A1. discovery_bucket = 0 rows for ALL probed symbols (manas_os/scanner/discovery.py) — Stage-1
    sensitive bucket is the safety net and catches nothing. Diagnose why (scoping/threshold/join
    bug) and fix so it surfaces near-miss practitioner names as counterfactual/WATCH, not empty.
A2. Trend-template hard-refuses recovery/EP names reclaiming the 200SMA (FCL/HIRECT/DAMCAPITAL —
    all closed just under 200SMA on a strong up-day). In scanner/gates.py gate_trend_template:
    the catalyst/EP + recovery families must relax the "close>SMA200" hard-fail into a SCORED
    OBJECTION when close is within a small band of the 200SMA AND the leg is a fresh strong move
    (reuse existing objection machinery + REVERSAL/EARLY_UPTREND family sets; do NOT relax
    base/pullback/momentum). Verify FCL/HIRECT/DAMCAPITAL on their dates now surface or carry the
    objection instead of hard-refuse; a genuine downtrend still refuses.
A3. Anticipation WATCH lens: SKIPPER/GENUSPOWER/INOXINDIA/LLOYDSENGG/KSHINTL are pre-trigger coils
    (Arora enters INSIDE the base w/ ~2% stop). Two sub-issues: (i) stop 6% > 5% cap because we
    compute breakout-distance stops on a not-yet-triggered name; (ii) quiet-coil delivery read as
    "distribution into trigger". Add a WATCH classification (not a candidate, not a refusal): a
    coiling name under a defined pivot with tight in-base stop math + armed trigger, delivery-z
    NOT penalized on low-range coil days. Surface via discovery_bucket/WATCH, one card style.
A4. EP measured-move: NUVOCO refused "R:R 0.70" — target collapses on gap days. Fix the
    measured-move/target for EP/gap setups so a real structural target is projected (box/leg
    height), R:R computed against it. Do NOT lower the 1.5 R:R floor.
Validation: re-run scan for 2026-07-15 & 2026-07-16, then the probe
(scratchpad/probe_labels.py) — target: >=11 of 13 labels surface OR sit in WATCH OR name a
defensible non-fixable gate. Log before/after to design/LEARNINGS.md.

## C. Data-freshness coherence (user 2026-07-18: "different data points on different dates, no accurate up-to-date status")
MEASURED root cause: two source families. bhavcopy-derived (prices/universe/breadth/regime/scan/
features/alpha/fundamentals/FII-DII) all fresh at 07-16. ChartsMaze-scrape-derived (screener_hits,
disclosures/deals, sector_metrics, industry_metrics) stuck at 07-14 — the ChartsMaze extractor
produced NO dump for 07-15/16, and the pipeline skipped silently. Panels mix both → mismatched dates.
Fixes:
C1. DATA-HEALTH / UPDATE-DIAGNOSTICS PANEL (highest value — user 2026-07-18: "clear indicator of
    the failure point, how and why ChartsMaze isn't updating"). ONE panel (shell "Data status"
    dropdown + a MARKET strip) that shows, per source, ALL of:
      - as-of date + GREEN(current)/AMBER(1 behind)/RED(2+ behind) dot,
      - LAST STATUS from the most recent update run (ok / skipped / failed) — read pipeline_runs
        (stage,status,detail) + jobs/job_steps (the Live Work durable log already captures this),
      - the EXACT REASON string verbatim (e.g. "ingest_chartsmaze: skip — folder missing:
        legacy/SwingEdge/data/chartsmaze/2026-07-15"),
      - a plain WHAT-TO-DO line per known failure (ChartsMaze folder-missing → "The ChartsMaze
        scraper hasn't produced files for these dates. Run the ChartsMaze extractor (login-gated)
        — screeners/sector-RS/deals stay at <date> until it does").
    Back it with GET /api/data/coverage; extend that endpoint to JOIN the latest pipeline_runs
    row per stage so status+reason+timestamp come through (freshness_map.py enumerates the
    sources; map each source → its producing stage name). The point: the user diagnoses staleness
    IN the tool — which source, which stage, what error, what to do — without reading logs.
C2. NO SILENT SOURCE-SKIP: a source stage that skips/fails must (a) show RED on the panel above
    with its reason, AND (b) raise a dismissable shell banner on affected screens ("ChartsMaze 2
    sessions behind — screeners/sector-RS/deals are as of <date>; see Data status"). The UPDATE
    button result (pipeline_run message) must also name any source that couldn't advance + why,
    not report a clean success when a source silently skipped.
C3. Reduce critical-feed dependence on the ChartsMaze middleman: deals (bulk/block) → NSE-direct
    daily source (same pattern as earnings_calendar) so the top-deals panel can't rot when the
    scrape fails. (Screeners/RS stay ChartsMaze for now — bigger dependency; C1/C2 make their lag
    honest.)
Done-test: coverage board renders all ~12 sources with correct as-of + dots on real data; forcing
a stale source shows RED + reason; deals panel pulls a fresh NSE-direct date independent of ChartsMaze.

## D. Scanner "no chart" thumbnails (user 2026-07-18)
ROOT CAUSE VERIFIED: ScannersTab ChartThumb (ScannersTab.jsx ~L163) renders <img src=chartUrl>
= GET /api/desk/chart, which serves a PRE-RENDERED PNG from data/agent_charts/<date>/<SYM>_<tf>.png
(app.py ~L4528). Those PNGs are generated only for the ~15 debate-council symbols (30 files for
07-16), but scanner rows are hundreds of arbitrary universe hits → 404 → "no chart" on nearly
every row by construction.
Fix: render the scanner-row thumbnail CLIENT-SIDE from GET /api/desk/chart-data (fetchChartData —
JSON OHLC from daily_prices, exists for any EQ symbol) as a small sparkline/mini-candle (reuse the
ChartDrawer's lightweight-charts or a compact SVG sparkline like ActivitySparkline already in
ScannersTab). Never depend on agent PNGs for universe symbols. Keep the full PNG/drawer on click.
Lazy-load per row (only fetch when row is near-viewport) to avoid hundreds of calls.
Done-test: scanner result rows show a real price thumbnail for arbitrary symbols (APARINDS/ATGL/
BANSALWIRE), not "no chart"; no 404 storm in the network tab.
Same fix applies anywhere else chartUrl/agent-PNG is used for non-council symbols (grep chartUrl).

## E. Debate free-model robustness + per-model status (user 2026-07-18: "why no openrouter free models in the panel")
VERIFIED: agents.models = [tencent/hy3:free, nvidia/nemotron-3-super-120b-a12b:free,
qwen/qwen3-next-80b-a3b-instruct:free, openai/gpt-oss-120b:free]; all 4 failed on the 07-16 run →
verdicts=0 → empty DEBATE panel. Distinct causes: gpt-oss 404 (dead/renamed OpenRouter id),
qwen 429 (free-tier throttle), tencent/nemotron returned non-JSON our strict parser rejected.
Free OpenRouter ids churn + rate-limit + don't honor JSON-schema.
Fixes (agents/debate.py + agents/_shared.py — NO money-math, additive):
E1. Lenient response parsing: strip ```json fences, extract the first balanced {...} block, and
    retry the call ONCE with a "return ONLY valid JSON" reprompt before counting a model failed.
    Handles tencent/nemotron non-JSON.
E2. 429 backoff: exponential retry (2-3 attempts) on HTTP 429 per model; a throttled model waits,
    doesn't just drop.
E3. Dead-id resilience: a 404 on one model must NOT blank the panel — skip it, record
    "unavailable (404 — model id retired)", and produce verdicts from whichever models DID answer.
    Chair proceeds on >=1 successful seat.
E4. PER-MODEL STATUS surfaced (ties into C diagnostics): DEBATE panel + the data-health board show
    each configured model with last-run outcome {ok N verdicts | 404 dead-id | 429 throttled |
    parse-fail | empty} — so an empty council is self-explaining, never a mystery. Store per-model
    status in the debate/pipeline log (already partially in the pipeline note) and read it back.
E5. Optional helper (backend, no config write): GET /api/agents/models/health that (when a key is
    present) lists the configured models + a live reachability probe, and flags retired ids — so
    the user knows which to refresh in config.yaml. Do NOT read/print/commit the api key; roster
    ids only. The roster itself stays user-managed in gitignored config.yaml (note in report:
    gpt-oss-120b:free is currently a dead id; user should replace with a current free id).
E6. PER-STOCK COST CAP (user 2026-07-18: "paid models OK if good/new + super cheap, not more than
    1 INR per stock; free preferred"). Add a config budget `agents.max_cost_inr_per_stock`
    (default 1.0) + `agents.usd_inr` (default ~86, configurable). We already capture OpenRouter
    `usage` (prompt/completion tokens) via _shared.unpack_chat. Fetch per-model pricing from
    OpenRouter /api/v1/models (cache daily; input+output $/token) → compute realized cost per
    (model × stock). Enforcement: (a) BEFORE calling a paid model, estimate cost from max_tokens ×
    price; if the running per-stock total would exceed the cap, SKIP that model with reason
    "over per-stock budget (est Rs X > cap Rs 1.00)". (b) Free models (:free, price 0) always
    allowed. (c) Show realized Rs cost per stock on the debate card + a nightly total. This lets
    the user drop in a cheap paid model (e.g. a deepseek-class chat model) and TRUST it can't blow
    past 1 INR/stock. Order roster free-first, paid-as-fallback, so paid only fires when free seats
    fail. DO NOT hardcode/recommend specific model ids or prices in code — read them live from
    OpenRouter; the roster stays user-managed in config.yaml.
Done-test: with 1 dead + 1 throttled model in the roster, the council still renders verdicts from
the survivors, and the panel shows per-model status incl. the failure reasons; a paid model in the
roster shows realized Rs/stock and is skipped (with reason) if it would exceed the 1 INR cap.

## F. Industry drill-down parity + discoverability (user 2026-07-18: "why can't we access
industrywise stock lists when clicking the sector")
VERIFIED LIVE: GET /api/desk/market/sector-stocks?sector=Pharmaceuticals works (165 stocks,
RS matches the user's ChartsMaze export exactly) — the capability exists but is buried + thin.
F1. Enrich the payload with columns the ChartsMaze per-industry export shows, ALL from data we
    already compute (features_daily / daily_prices): pct_from_52w_high, day_range_pct, rvol,
    adr14_pct, adr20_pct. No new ingest.
F2. Fix payload bugs seen live: some rows return empty close (join gap — e.g. BLISSGVS,
    VENUSREM had rs but no close); run_date field comes back blank. Every row gets close+1d%
    or an honest null reason; run_date always populated + surfaced as the RS as-of stamp
    (ChartsMaze RS may lag bhavcopy — stamp it, per the freshness rules).
F3. DISCOVERABILITY: every industry/sector label across the desk becomes a click-through to this
    drill-down panel — MARKET sector/breadth tables, setup-card sector chips, ALPHA sector column,
    SCANNERS rows. One shared drawer/panel, sortable by RS (default) and any column.
Done-test: clicking "Pharmaceuticals" anywhere opens the full member list with RS / %off-52wH /
RVOL / ADR columns, no empty closes, an RS as-of stamp, sortable; parity spot-check vs the
user's Stocks Data_Pharmaceuticals.csv (BLISSGVS 99 / NOVARTIND 99 / INDSWFTLAB 98).

## G. Sector strength + LONGEVITY factors (user 2026-07-18: "instead of empty columns like MARS,
show factors for strength AND longevity potential of sectors") — wave-3 pool, pairs with F
MARS empty-cause: Fyers sector-RS ingest failed while token dead (sectors_done=0 failed=15); may
self-heal now token is live, but the design rule stands: NEVER render an empty column — MARS
becomes a bonus column when present, and the table leads with factors computed from OUR data
(daily_prices + screener_hits RS + universe sectors — no new ingest):
STRENGTH (today):
  - members adv% today; % above 20/50/200 DMA (sector breadth quality)
  - leadership breadth: # members RS>=80, # within 5% of 52wH, new-52wH count (broad vs 2-stock rally)
  - volume rush concentration: # members with RVOL>=2 / HVQ-class volume days this week
LONGEVITY / STAGE (the durability read — the N.E.X.T "Dull->Accelerating" Evaluate concept):
  - RS-trend: sector median-RS slope over 1W and 1M (rising = early, flattening = mature)
  - acceleration: 1W-rank vs 1M-rank vs 3M-rank ordering -> stage tag
    {DULL | ACCELERATING (1W>1M>3M) | LEADING (top across all) | FADING (1W < 1M/3M)}
  - leadership age: consecutive sessions the sector has been top-quartile RS (young leg = room;
    long-led = late) 
  - extension risk: median member distance from 20DMA vs sector-median ADR (stretched = late-stage)
One glance answer: "strong AND early" vs "strong but late". Stage tag renders as the hero chip per
sector row; factors as sortable columns; stamped with their as-of dates per freshness rules. All
display/evidence — no gate changes. Replay note: stage-tag transitions (DULL->ACCELERATING) get
logged so Q-hygiene can later test their forward edge (the stocksgeeks 'highest-conviction signal').
Done-test: sector table has zero permanently-empty columns; each sector shows stage chip +
strength/longevity columns computed from real data; MARS appears only when its ingest succeeded.

Sequence: C1/C2 (freshness board) is independent + high-value — can lead. D (scanner thumbs) and
E (debate robustness) are independent, safe in parallel. B (isolated, safe) next. F+G ride wave 3
(same sector-table surface + freshness machinery).
A2/A4 are gate edits (touch gates.py + tests); A1/A3 are discovery.py + a WATCH surface. One Codex
job; report per item. NOTE: ChartsMaze catch-up for 07-15/16 needs the Playwright extractor re-run
(login-gated) — separate from code; flag in report if dumps still missing.

## H. (wave-3 pool) Screener-side confluence + honesty stats (user screenshots 2026-07-18)
- H1 'Featured in N screens': per-symbol count of distinct preset screeners hit for the date (GROUP BY over screener_hits), sortable column on SCANNERS + a chip on cards; folds into the EDGE-STACK score as the screener-confluence input.
- H2 Per-preset calibration stats on each SCANNERS card from the existing screener_calibration cells: 'hits: X% positive T+10 (n=Y)' -- measured record, not a claim. No new compute.
- H3 (low prio backlog) P/E-vs-sector-PE context chip on catalyst/EP cards via yfinance ratios; skip ROE/ROCE/div tiles (investor decor, not swing signals). NL-to-screen layer: skipped for now (LLM cost/fragility; presets+guided flow cover beginners).
- H4 'NEW TONIGHT' delta strip (user workflow paste 2026-07-18): top of PREPARE/WATCH shows entrants/exits/upgrades vs prior session (data exists: curator_delta, scan-date diffs, watchlist status history) -- the daily ritual keys on what CHANGED.
- H5 clean-catalyst chip: 'no adverse disclosure/result last 5 sessions' on cards from disclosures/announcements (rides results-intelligence); negative hit renders the reason.
- A5 (wave-3, from hot-stocks probe 2026-07-18): 'no measured move -- R:R unknowable' refusal class on near-52wH CONTINUATION names (FEDERALBNK/BHARATFORG/TORNTPHARM; BHEL/CANFINHOME R:R~0). Extend A4's structural-target fix beyond EP: when price is within 5pct of 52wH with no overhead structure, project target = prior-leg height or k*ADR (replay-pick k), R:R against that; never refuse a leader solely because nothing is overhead. Keep 1.5 floor.
- H6 (tradl review 2026-07-18): breadth LEADERBOARD over featured-in-N-screens (sortable universe rank + 1D/1W/1M/1Y) once H1 ships; 'since featured in' timestamp+running-%% badge on preset rows (UI pattern for calibration/capture stats); cascading multi-screen browse view; category+bias tags on presets. SKIP: QVMG letter badges, fundamentals terminal, indicator wall.
- H7 news sentiment tags (net-new, tightly scoped): headlines for WATCHLIST/POSITION symbols only, cheap LLM bullish/bearish+impact tag, evidence-only, symbol page + journal. Requires new ingest -- smallest source first.
- OPS NOTE: chartsmaze extractor session_invalid (login expired) = the 07-15..17 dump gap. User must run chartsmaze_extractor/login.py once; nightly task then keeps it fresh. Data-health panel carries this exact failure + what-to-do.
