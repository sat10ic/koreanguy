[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "manas-os"
version = "0.0.1"
description = "Manas AI Trading OS"
requires-python = ">=3.11"

[project.scripts]
manas = "manas_os.cli:main"

[tool.setuptools.packages.find]
include = ["manas_os*"]
